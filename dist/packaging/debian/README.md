# Debian Packaging

Place Debian/Ubuntu packaging files here:

- `debian/control` - Package metadata and dependencies
- `debian/rules` - Build rules (dh-based)
- `debian/changelog` - Debian changelog
- `debian/install` - File installation paths
- `debian/source/format` - Source format (usually "3.0 (quilt)")
